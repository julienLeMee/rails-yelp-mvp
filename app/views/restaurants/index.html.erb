<h1 class="m-3">Restaurants</h1>

<div>
  <% @restaurants.each do |restaurant| %>
    <h3 class="m-3"><%= link_to restaurant.name, restaurant_path(restaurant) %></h3>
  <% end %>
</div>

<%= link_to "New restaurant", new_restaurant_path %>
