<%= simple_form_for(@restaurant) do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :category, :collection => %w[chinese italian japanese french belgian] %>
    <%= f.input :phone_number %>
  </div>

  <div class="form-actions btn btn-alert">
    <%= f.button :submit %>
  </div>
<% end %>
